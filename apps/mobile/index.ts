import "./src/i18n";
// Defines the strike notification background task in the global scope so it
// is available when the OS launches the app headlessly.
import "./src/notifications/strike-notifications";
import "expo-router/entry";
